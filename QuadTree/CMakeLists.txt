PROJECT(QuadTree)
SET( ${PROJECT_NAME}_SOURCES
  quadtree_example.cpp
)

ADD_EXECUTABLE(${PROJECT_NAME} ${${PROJECT_NAME}_SOURCES})
TARGET_LINK_LIBRARIES( ${PROJECT_NAME} ${SDL_LIBRARY} ${OPENGL_gl_LIBRARY})
