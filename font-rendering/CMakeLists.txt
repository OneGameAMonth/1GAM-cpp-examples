PROJECT(Font-rendering)
SET( ${PROJECT_NAME}_SOURCES
  font.cpp
  glref.cpp
)

ADD_EXECUTABLE(${PROJECT_NAME} ${${PROJECT_NAME}_SOURCES})
TARGET_LINK_LIBRARIES( ${PROJECT_NAME} ${SDL_LIBRARY} ${FREETYPE_LIBRARIES} ${OPENGL_gl_LIBRARY} ${PNG_LIBRARIES} ${FONTCONFIG_LIBRARIES})
